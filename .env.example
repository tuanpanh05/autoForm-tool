# AI Provider Keys (optional, only needed for v2+ AI features)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Browser settings
BROWSER_HEADLESS=false
BROWSER_TYPE=chromium
